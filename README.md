
  # Recreate Landing Page Hero

  This is a code bundle for Recreate Landing Page Hero. The original project is available at https://www.figma.com/design/osSDLjYVVtAjHbcPeD8eQf/Recreate-Landing-Page-Hero.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  